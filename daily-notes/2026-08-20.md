# 20 August 2026 — Advanced Python - 1

Today I started working through **Advanced Python - 1**.

This chapter introduced a lot of smaller Python features that I hadn't really used before. Some of them were pretty straightforward, while others will probably need more practice before I can use them naturally.

## What I learned

Today I learned about:

- Walrus operator `:=`
- Type definitions
- `match` and `case`
- Merging things in Python
- Opening multiple files using `with`
- Exception handling
- Raising exceptions
- `try` with `else`
- `try` with `finally`
- Importing modules
- The `global` keyword
- `enumerate()`
- List comprehensions

## Practice

I completed the practice set for this chapter.

I practiced handling multiple files with `try` and `except`, using `enumerate()` to access specific elements of a list, creating multiplication tables with list comprehensions, and handling `ZeroDivisionError`.

I also practiced writing data to a file using `with open()`.

The file-handling and exception-handling parts were especially useful because they showed me how Python programs can deal with errors without immediately stopping the entire program.

## What I understood

One thing I noticed in this chapter is that Python has a lot of different ways to make code shorter or more flexible.

For example, list comprehensions can create a list in a much shorter way than writing a complete loop, and `enumerate()` makes it easier to work with both the index and value while looping.

Exception handling was another important part of this chapter. I learned how `try`, `except`, `else`, and `finally` work together and how exceptions can be handled instead of allowing the program to crash.

I also learned that exceptions can be raised manually when I want to make sure something is valid.

## Progress

This chapter was a good step forward from the Python fundamentals I've been learning so far.

There are still quite a few things from this chapter that I don't feel completely comfortable with yet, especially some of the newer syntax and when I should actually use certain features.

I don't want to just memorize the syntax, so I'll keep using these concepts in practice and future projects.

**Another chapter completed. Slowly getting more comfortable with Python and moving into more advanced concepts.**