# 24 August 2026 — Advanced Python 2

Today I continued learning **Advanced Python - 2**.

This chapter introduced some useful Python features and also helped me understand how virtual environments and packages are managed.

## What I learned

Today I learned about:

- Virtual environments (`venv`)
- `pip freeze`
- Lambda functions
- The `join()` method
- The `format()` function
- `map()`
- `filter()`
- `reduce()`

## Practice

I practiced creating virtual environments and learned how `pip freeze` can be used to create a `requirements.txt` file containing the packages installed in an environment.

I also practiced formatting strings using `format()` and joining elements together using `join()`.

For functional programming, I worked with `lambda`, `map()`, `filter()`, and `reduce()`.

I used `filter()` to find numbers divisible by 5 and `reduce()` to find the maximum number from a list.

I also explored Flask as part of the practice and created a very basic Python web server that returns a "Hello, World!" message.

## What I think so far

Some of these functions are still pretty new to me, especially `lambda`, `map()`, and `reduce()`. I understand the basic idea, but I think I'll need to use them more before they become natural.

The virtual environment part was also useful because I can now see how Python projects can keep their packages separated and how `requirements.txt` can be used to recreate an environment.

Overall, this chapter introduced a lot of small but useful Python features.

## Progress

Another Advanced Python chapter completed.

I'm slowly getting more comfortable with Python and starting to learn features that will be useful when I begin working on larger projects.

I still have plenty to practice, but I'll keep building on what I've learned.