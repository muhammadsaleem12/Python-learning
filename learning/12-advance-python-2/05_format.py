'''FORMAT METHOD (STRINGS)'''
# Formats the values inside the string into a desired output.

a = "{} is a good {}".format("Chris", "boy") 

print(a)

# Format method is an old method that was used a while back, Untill "f" string took over.